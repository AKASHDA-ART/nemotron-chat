kind = "web"
previewPath = "/"
title = "Nemotron Chat"
version = "1.0.0"
id = "artifacts/nemotron-chat"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24899

[services.development]
run = "pnpm --filter @workspace/nemotron-chat run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nemotron-chat", "run", "build" ]
publicDir = "artifacts/nemotron-chat/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24899"
BASE_PATH = "/"
