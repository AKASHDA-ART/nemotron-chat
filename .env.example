# NVIDIA Integrate API (required for chat completions)
NVIDIA_API_KEY=your_key_here

# API server port (default 8080)
PORT=8080

# Optional: SQLite database file path (default: ./data/local.db)
# DATABASE_URL=file:./data/local.db
